backend:
  name: git-gateway
  branch: wip/react
media_folder: "static/uploads" # Media files will be stored in the repo under images/uploads
public_folder: "/uploads" # Media files will be stored in the repo under images/uploads
collections:
- name: "blog" # Used in routes, e.g., /admin/collections/blog
  label: "Blog" # Used in the UI
  folder: "content/blog" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", required: false}
    - {label: "Publish Date", name: "date", widget: "datetime", required: false}
    - {label: "Author", name: "author", widget: "string", required: false}
    - {label: "Author URL", name: "author_url", widget: "string", required: false}
    - {label: "Preview Image", name: "preview_image", widget: "image", required: false}
    - {label: "Body", name: "body", widget: "markdown", required: false}
- name: "patient-resources" # Used in routes, e.g., /admin/collections/blog
  label: "Patient Resources" # Used in the UI
  folder: "content/patient-resources" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", required: false}
    - {label: "Category", name: "category", widget: "string", required: false}
    - {label: "Preview Image", name: "preview_image", widget: "image", required: false}
    - {label: "Body", name: "body", widget: "markdown", required: false}
- name: "who-we-are" # Used in routes, e.g., /admin/collections/blog
  label: "Who We Are" # Used in the UI
  folder: "content/who-we-are" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", required: false}
    - {label: "Job Title", name: "jobtitle", widget: "string", required: false}
    - {label: "Photo", name: "photo", widget: "image", required: false}
    - {label: "Body", name: "body", widget: "markdown", required: false}
    - {label: "Skill 1", name: "skill1", widget: "string", required: false}
    - {label: "Rating 1", name: "rating1", widget: "number", valueType: "int", required: false}
    - {label: "Skill 2", name: "skill2", widget: "string", required: false}
    - {label: "Rating 2", name: "rating2", widget: "number", valueType: "int", required: false}
    - {label: "Skill 3", name: "skill3", widget: "string", required: false}
    - {label: "Rating 3", name: "rating3", widget: "number", valueType: "int", required: false}
    - {label: "Skill 4", name: "skill4", widget: "string", required: false}
    - {label: "Rating 4", name: "rating4", widget: "number", valueType: "int", required: false}
- name: "what-we-do" # Used in routes, e.g., /admin/collections/blog
  label: "What We Do" # Used in the UI
  folder: "content/what-we-do" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", required: false}
    - {label: "Category", name: "category", widget: "string", required: false}
    - {label: "Preview Image", name: "preview-image", widget: "image", required: false}
    - {label: "Body", name: "body", widget: "markdown", required: false}
