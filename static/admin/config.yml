backend:
  name: git-gateway
media_folder: "static/uploads" # Media files will be stored in the repo under images/uploads
public_folder: "/uploads" # Media files will be stored in the repo under images/uploads
collections:
- name: "blog" # Used in routes, e.g., /admin/collections/blog
  label: "Blog" # Used in the UI
  folder: "content/blog" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", optional: true}
    - {label: "Publish Date", name: "date", widget: "datetime", optional: true}
    - {label: "Author", name: "author", widget: "string", optional: true}
    - {label: "Author URL", name: "author_url", widget: "string", optional: true}
    - {label: "Preview Image", name: "preview_image", widget: "image", optional: true}
    - {label: "Body", name: "body", widget: "markdown", optional: true}
- name: "patient-resources" # Used in routes, e.g., /admin/collections/blog
  label: "Patient Resources" # Used in the UI
  folder: "content/patient-resources" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", optional: true}
    - {label: "Category", name: "category", widget: "string", optional: true}
    - {label: "Preview Image", name: "preview_image", widget: "image", optional: true}
    - {label: "Body", name: "body", widget: "markdown", optional: true}
- name: "who-we-are" # Used in routes, e.g., /admin/collections/blog
  label: "Who We Are" # Used in the UI
  folder: "content/who-we-are" # The path to the folder where the documents are stored
  create: true # Allow users to create new documents in this collection
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields: # The fields for each document, usually in front matter
    - {label: "Title", name: "title", widget: "string", optional: true}
    - {label: "Job Title", name: "jobtitle", widget: "string", optional: true}
    - {label: "Photo", name: "photo", widget: "image", optional: true}
    - {label: "Body", name: "body", widget: "markdown", optional: true}
    - {label: "Skill 1", name: "skill1", widget: "string", optional: true}
    - {label: "Rating 1", name: "rating1", widget: "number", valueType: "int", optional: true}
    - {label: "Skill 2", name: "skill2", widget: "string", optional: true}
    - {label: "Rating 2", name: "rating2", widget: "number", valueType: "int", optional: true}
    - {label: "Skill 3", name: "skill3", widget: "string", optional: true}
    - {label: "Rating 3", name: "rating3", widget: "number", valueType: "int", optional: true}
    - {label: "Skill 4", name: "skill4", widget: "string", optional: true}
    - {label: "Rating 4", name: "rating4", widget: "number", valueType: "int", optional: true}